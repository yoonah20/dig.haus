node_modules/
dist/
.env
*.db
*.db-shm
*.db-wal
server/data/
db-dump/

# TypeScript incremental build cache (regenerated every build,
# shouldn't be tracked).
*.tsbuildinfo

# Seed DB ships with the repo so fresh boots / empty volumes have schema + data
!server/seed/
!server/seed/*.db
.DS_Store

# IDE / OS
.vscode/
.idea/
*.log

# Local Claude Code settings
.claude/settings.local.json

# Local-only files (per-developer, not shared)
/dev
/docs/operator-portrait.md
